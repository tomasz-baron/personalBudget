import { TransactionCategoryPipe } from './transaction-category.pipe';

describe('TransactionCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
